<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Required to draw the floating bubble on top of other apps. User must
         grant this in system settings (Settings > Apps > Special access >
         Display over other apps). See docs/p1c-permission-setup.md §2. -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />

    <!-- Required for FloatingBubbleService (T30.5) to keep the bubble alive
         when the user is in another app. Foreground type "specialUse" lets us
         declare the legitimate purpose to Android 14+. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application>
        <service
            android:name=".service.StyleMirrorAccessibilityService"
            android:exported="false"
            android:label="@string/p1c_accessibility_service_label"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_service_config" />
        </service>

        <!--
            FloatingBubbleService (T30.5). foregroundServiceType="specialUse"
            is required on Android 14+; the <property> declares the
            human-readable subtype Google's review process expects.
        -->
        <service
            android:name=".service.FloatingBubbleService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="floating chat assistant overlay" />
        </service>
    </application>
</manifest>
